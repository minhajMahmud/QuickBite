name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.4'
      SERVERPOD_PASSWORD_database: 'Wcn8Z_sxN096mAd_vM9b8yy57tr0ywuF'
      SERVERPOD_PASSWORD_redis: 'teBZBLVz4WgcMZnKYW0MGWRJNxoiml-6'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: quickbiteserver_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: quickbiteserver_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: quickbiteserver_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: quickbiteserver_server
        run: serverpod generate

      - name: Run tests
        working-directory: quickbiteserver_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: quickbiteserver_server
        run: docker compose down -v
